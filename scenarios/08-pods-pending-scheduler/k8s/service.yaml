---
apiVersion: v1
kind: Service
metadata:
  name: scenario-08-pending-scheduler
  namespace: default
  labels:
    scenario: "08-pods-pending-scheduler"
spec:
  selector:
    app: scenario-08
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
